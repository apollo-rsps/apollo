subprojects {
    if (it.buildFile.exists()) {
        apply plugin: 'apollo-plugin'

        repositories {
            mavenCentral()
            maven {
                url { 'https://dl.bintray.com/kotlin/kotlinx/' }
            }
        }
    }
}
